const getters = {
  village: state => state.app.village,
  loading: state => state.app.loading,
  toast: state => state.app.toast,
  timeShow: state => state.app.timeShow,
  timeChoose: state => state.app.timeChoose
}
export default getters
